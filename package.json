{
  "name": "flowscope-monorepo",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "vercel-build": "cd client && npm install && npm run build && cd .. && rm -rf dist && cp -R client/dist dist"
  }
}
